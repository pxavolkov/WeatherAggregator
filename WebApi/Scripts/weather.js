(function () {

    // use a "strict" operating context. This strict context prevents certain actions from being taken and throws more exceptions.
    'use strict';

    // Declare app level module
    angular.module('weatherApp', ['ngSanitize']);
})();
